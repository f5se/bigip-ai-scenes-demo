name: llm-router-demo
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi>=0.115.0
      - uvicorn[standard]>=0.32.0
      - httpx>=0.27.0
      - pydantic-settings>=2.6.0
      - jinja2>=3.1.0
